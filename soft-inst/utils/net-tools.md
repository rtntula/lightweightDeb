# Ностальгия по ipconfig и проч. -- Net-tools

> [`<<`](../index.md)

Includes: arp, hostname, ifconfig, netstat, rarp, route, plipconfig, slattach, mii-tool and iptunnel and ipmaddr.

```  
$ sudo apt install net-tools -y
$ sudo ifconfig
```
